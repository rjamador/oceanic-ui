# eslint-plugin-jsx-a11y@6.10.2 still declares its eslint peer as "<= ^9"
# while this repo is on eslint 10. The plugin works fine on 10 (pure AST,
# flat-config native). Drop this line once jsx-a11y widens its peer range.
legacy-peer-deps=true
